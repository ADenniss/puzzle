# Last Light

Last Light is a timed bunker puzzle game about reading transmissions, solving changing lever codes, and keeping the power on until sunrise.

To test locally, run:

```sh
python3 -m http.server --bind 0.0.0.0 8010
```

Then open http://localhost:8010.
